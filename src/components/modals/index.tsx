import { LogInModal } from './LogInModal';
import { SignUpModal } from './SignUpModal';

export const Modals: React.FC = () => {
  return (
    <>
      <LogInModal />
      <SignUpModal />
    </>
  );
};
